# 互联网医院 MVP 验证记录

## 后端

- [ ] `mvn test` 全量通过。
- [ ] PostgreSQL 16 成功执行 `resources/sql/init.sql`。
- [ ] 网关将认证后的租户请求透传 `X-Tenant-Id`。
- [ ] 号源锁定逻辑不会超卖。
- [ ] 便民门诊抢单只能成功一次。
- [ ] 问诊 WebSocket 能保存并广播文本消息。
- [ ] 问诊超时调度能将超时问诊更新为 `TIMEOUT`。
- [ ] 处方审核会发布 `prescription.audited` 事件。
- [ ] 模拟支付会发布 `order.paid` 事件。

## 前端

- [ ] `pnpm --filter admin-web build` 通过。
- [ ] `pnpm --filter patient-h5 build` 通过。
- [ ] 管理端可导航到租户、医生、预约、处方、药品和订单页面。
- [ ] 患者端可完成医院、科室、医生、预约确认和问诊聊天主流程浏览。

## 人工冒烟链路

1. 管理端登录后进入工作台，检查菜单、医生、预约、处方、药品、订单页面结构是否完整。
2. 患者端进入首页后依次浏览医院、科室、医生详情、预约确认和问诊聊天页面。
3. 后端联调时验证处方审核、药品发货、订单支付三个关键事件链路。

## 当前说明

- 本轮按仓库要求未执行编译和测试，以上清单尚未实际勾选。
- 本文档用于后续端到端联调时逐项补充验证结果。
