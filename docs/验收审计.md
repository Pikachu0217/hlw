# 基础管理模块逐项验收审计

## 审计口径

本审计用于把总控模式原始要求逐项映射到当前仓库证据。受项目约束“不要编译测试”限制，本次只使用静态证据，不执行 Maven、pnpm build/test、接口脚本或运行联调命令。

## 要求与证据

| 要求 | 当前状态 | 证据 |
| --- | --- | --- |
| 全面检查数据库表结构 | 已完成静态审计 | `resources/sql/init.sql` 统一覆盖 30 张完整 baseline 表；SQL 的表注释和字段注释缺失数均为 0。 |
| 全面检查后端接口 | 已完成静态审计 | Controller 静态扫描得到 65 个 HTTP endpoint；`resources/scripts/api-test.sh` 覆盖所有 HTTP endpoint，登录接口由 `run_login_case` 单独处理。 |
| 全面检查前端页面 | 已完成静态审计 | `admin-web` 路由覆盖工作台、租户、系统管理、医生、科室、患者、咨询、预约、处方、药品和订单页面。 |
| 用户管理 | 已落地 | 后端 `GET/POST /system/users`，前端 `/system/users`，数据库 `sys_user`，接口脚本包含查询和创建用例。 |
| 角色管理 | 已落地 | 后端 `GET/POST /system/roles`，前端 `/system/roles`，数据库 `sys_role`，接口脚本包含查询和创建用例。 |
| 菜单管理 | 已落地 | 后端 `GET/POST /system/menus`，前端 `/system/menus`，数据库 `sys_menu`，接口脚本包含查询和创建用例。 |
| 科室管理 | 已落地 | 后端 `GET/POST /doctor/departments`，前端 `/doctor/departments`，数据库 `doc_department`，接口脚本包含查询和创建用例。 |
| 权限管理 | 已落地 | 后端 `GET/POST /system/permissions`，前端 `/system/permissions`，数据库 `sys_permission`，接口脚本包含查询和创建用例。 |
| 岗位管理 | 已落地 | 后端 `GET/POST /system/posts`，前端 `/system/posts`，数据库 `sys_post`，接口脚本包含查询和创建用例。 |
| 字典管理 | 已落地 | 后端 `GET/POST /system/dicts`，前端 `/system/dicts`，数据库 `sys_dict`，接口脚本包含查询和创建用例。 |
| 数据库已设计但未落地模块 | 已梳理 | `con_consult_image` 为完整 baseline 预留表，因当前无图片上传地址和图片排序入参，暂不启用；其余联调用表均有业务映射或本地消息用途。 |
| 数据库业务逻辑实现 | 已落地 | 系统、医生、患者、预约、问诊、处方、药品、订单、本地消息均有 JDBC 或业务服务路径；历史内存仓储保留用于轻量上下文和测试。 |
| 后端接口开发 | 已落地 | `docs/交付总结.md` 已列出新增接口清单；Controller 静态扫描与接口脚本覆盖一致。 |
| 前端页面开发 | 已落地 | `code/frontend/admin-web/src/router/router.tsx` 和 `navigation.tsx` 已配置菜单和路由入口。 |
| 权限控制接入 | 已落地基础能力 | 已实现权限码、用户角色、角色菜单接口和菜单数据；网关租户透传和登录保护已接入管理端路由。 |
| 菜单配置接入 | 已落地 | `sys_menu` 初始化数据包含系统管理、医生管理和科室入口；前端导航同步配置。 |
| 数据校验与异常处理 | 已落地 | 业务服务使用 `BizException` 做参数和状态校验；各服务存在 `*ExceptionHandler` 统一返回错误响应。 |
| 分析表关联关系 | 已完成并记录 | 用户-角色、角色-菜单、用户-岗位、医生-科室、预约-号源、问诊-消息、处方-明细、药品-库存/配送、订单-业务单据均在 README 和交付总结中说明。 |
| 每个模块完成后代码审查和提交 | 已按阶段提交 | Git 记录包含基础管理、医生科室、患者、预约、问诊、处方、药品、订单、系统核心收口、前端新增、审计收口、方法注释和交付文档等提交。 |
| 编译后端并修复错误 | 未执行 | 项目指令明确要求“不要编译测试”，因此未运行 Maven 编译。 |
| 编译前端并修复错误 | 未执行 | 项目指令明确要求“不要编译测试”，因此未运行 pnpm build/test。 |
| 联调前后端接口 | 未执行 | 项目指令明确要求“不要编译测试”，因此未启动服务或执行接口脚本。 |
| 验证新增功能可正常使用 | 静态验证完成，运行验证未执行 | 已完成接口脚本覆盖、SQL 注释、方法注释、前端样式、路由菜单等静态检查；运行级验证受约束未执行。 |
| 新增接口后更新接口测试脚本 | 已完成 | `resources/scripts/api-test.sh` 覆盖系统、医生、患者、预约、问诊、处方、药品、订单和 WebSocket 跳过说明。 |
| 新增功能更新 README | 已完成 | `code/backend/README.md`、`code/frontend/README.md`、`docs/交付总结.md` 均已更新。 |

## 当前静态检查结果

- public 方法 Javadoc 扫描：`missing_public_method_javadocs=0`。
- Javadoc 与注解顺序扫描：`annotation_before_javadoc=0`。
- 前端内联样式扫描：未发现 `style={{...}}`。
- SQL 注释扫描：`resources/sql/init.sql` 无表注释和字段注释缺失。
- Git 工作树：仅 `resources/scripts/service.sh` 存在用户未提交改动，本次任务未提交该文件。

## 结论

基础管理模块及数据库、后端、前端、权限菜单、文档和提交要求已完成静态交付。完整运行级验证仍受“不要编译测试”约束阻止，不能宣称已通过编译、构建或联调。
